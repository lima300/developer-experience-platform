pre-commit:
  parallel: true
  commands:
    lint-staged:
      run: pnpm lint-staged

pre-push:
  parallel: true
  commands:
    type-check:
      run: pnpm turbo type-check
    test:
      run: pnpm turbo test
